// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef XPHASE_H
#define XPHASE_H

#ifdef __cplusplus
extern "C" {
#endif

/***************************** Include Files *********************************/
#ifndef __linux__
#include "xil_types.h"
#include "xil_assert.h"
#include "xstatus.h"
#include "xil_io.h"
#else
#include <stdint.h>
#include <assert.h>
#include <dirent.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>
#include <unistd.h>
#include <stddef.h>
#endif
#include "xphase_hw.h"

/**************************** Type Definitions ******************************/
#ifdef __linux__
typedef uint8_t u8;
typedef uint16_t u16;
typedef uint32_t u32;
#else
typedef struct {
    u16 DeviceId;
    u32 Cntrl_BaseAddress;
} XPhase_Config;
#endif

typedef struct {
    u32 Cntrl_BaseAddress;
    u32 IsReady;
} XPhase;

/***************** Macros (Inline Functions) Definitions *********************/
#ifndef __linux__
#define XPhase_WriteReg(BaseAddress, RegOffset, Data) \
    Xil_Out32((BaseAddress) + (RegOffset), (u32)(Data))
#define XPhase_ReadReg(BaseAddress, RegOffset) \
    Xil_In32((BaseAddress) + (RegOffset))
#else
#define XPhase_WriteReg(BaseAddress, RegOffset, Data) \
    *(volatile u32*)((BaseAddress) + (RegOffset)) = (u32)(Data)
#define XPhase_ReadReg(BaseAddress, RegOffset) \
    *(volatile u32*)((BaseAddress) + (RegOffset))

#define Xil_AssertVoid(expr)    assert(expr)
#define Xil_AssertNonvoid(expr) assert(expr)

#define XST_SUCCESS             0
#define XST_DEVICE_NOT_FOUND    2
#define XST_OPEN_DEVICE_FAILED  3
#define XIL_COMPONENT_IS_READY  1
#endif

/************************** Function Prototypes *****************************/
#ifndef __linux__
int XPhase_Initialize(XPhase *InstancePtr, u16 DeviceId);
XPhase_Config* XPhase_LookupConfig(u16 DeviceId);
int XPhase_CfgInitialize(XPhase *InstancePtr, XPhase_Config *ConfigPtr);
#else
int XPhase_Initialize(XPhase *InstancePtr, const char* InstanceName);
int XPhase_Release(XPhase *InstancePtr);
#endif

void XPhase_Start(XPhase *InstancePtr);
u32 XPhase_IsDone(XPhase *InstancePtr);
u32 XPhase_IsIdle(XPhase *InstancePtr);
u32 XPhase_IsReady(XPhase *InstancePtr);
void XPhase_EnableAutoRestart(XPhase *InstancePtr);
void XPhase_DisableAutoRestart(XPhase *InstancePtr);

void XPhase_Set_rincdec_V(XPhase *InstancePtr, u32 Data);
u32 XPhase_Get_rincdec_V(XPhase *InstancePtr);

void XPhase_InterruptGlobalEnable(XPhase *InstancePtr);
void XPhase_InterruptGlobalDisable(XPhase *InstancePtr);
void XPhase_InterruptEnable(XPhase *InstancePtr, u32 Mask);
void XPhase_InterruptDisable(XPhase *InstancePtr, u32 Mask);
void XPhase_InterruptClear(XPhase *InstancePtr, u32 Mask);
u32 XPhase_InterruptGetEnabled(XPhase *InstancePtr);
u32 XPhase_InterruptGetStatus(XPhase *InstancePtr);

#ifdef __cplusplus
}
#endif

#endif
